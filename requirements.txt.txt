# Core numerical / data
numpy>=1.24
pandas>=1.5
scipy>=1.10

# Machine learning / preprocessing
scikit-learn>=1.2
joblib>=1.2

# Survival analysis
lifelines>=0.27
scikit-survival>=0.16  # pip package name: scikit-survival

# Imputation / decomposition (already in sklearn, listed for clarity)
# KNNImputer, PCA included in scikit-learn

# Visualization
matplotlib>=3.6
seaborn>=0.12
plotly>=5.15

# Web UI
streamlit>=1.22

# Excel / file IO
openpyxl>=3.1
xlrd>=2.0

# Document generation
python-docx>=0.8.11

# Optional / helpful
tqdm>=4.65
pytest>=7.0   # if you include tests

# Notes:
# - scikit-survival (scikit-survival) often requires a working C/C++ toolchain or pre-built wheels.
#   If you use conda, installing via `conda install -c conda-forge scikit-survival` is recommended.
